# Frontend for Custom Design Database

## To Run Locally
1. Clone directory on your local machine
2. Run `npm install` in the directory
3. Run `npm start`
(You'll need to simultaneously run the backend for the local calls to work)